= simple_form_for comment, url: post_comment_path(@post, comment) do |f|
  = f.input :content, label: false, placeholder: 'Type here ...'

  = f.button :submit, 'Update comment', class: 'btn btn-success mt-4 btn-block'
